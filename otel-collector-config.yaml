receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "0.0.0.0:4319"
      http:
        endpoint: "0.0.0.0:4320"

processors:
  batch: {}

exporters:
  logging:
    loglevel: info
  #todo: add jaeger config
  # jaeger:
  #   endpoint: "jaeger:14250"
  #   tls:
  #     insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [logging]
